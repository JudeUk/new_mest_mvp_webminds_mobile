import 'package:flutter/material.dart';

class MentorFavorites extends StatelessWidget {
  const MentorFavorites({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue[100],
    );
  }
}